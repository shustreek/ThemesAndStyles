<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.ThemesAndStyles" parent="Base.Theme.ThemesAndStyles">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Theme.ThemesAndStyles.Main" parent="Base.Theme.ThemesAndStyles.V29"/>

    <style name="Base.Theme.ThemesAndStyles.V29" parent="Base.SimpleTheme.ThemesAndStyles">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>